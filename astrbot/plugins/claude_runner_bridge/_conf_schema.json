{
  "agent_runner_base_url": {
    "type": "string",
    "title": "Agent Runner Base URL",
    "description": "Base URL for the local Rust agent-runner HTTP service.",
    "default": "http://127.0.0.1:8787"
  },
  "default_cwd": {
    "type": "string",
    "title": "Default Runner CWD",
    "description": "Working directory sent to agent-runner for Claude execution.",
    "default": "/workspace"
  },
  "default_timeout_secs": {
    "type": "int",
    "title": "Default Timeout Seconds",
    "description": "Per-request timeout forwarded to agent-runner.",
    "default": 120,
    "minimum": 1,
    "maximum": 300
  },
  "command_name": {
    "type": "string",
    "title": "Agent Command Name",
    "description": "Chat command that triggers the runner bridge.",
    "default": "agent"
  },
  "status_command_name": {
    "type": "string",
    "title": "Status Command Name",
    "description": "Chat command that checks agent-runner health.",
    "default": "agent-status"
  }
}
